import React from "react";
import ReactDOM from 'react-dom';
import Http from "./common/http";
import ApiUrl from "./common/api-url";
import classNames from 'classnames';

import { Row, Col, Button, Select, Space
} from 'antd';

import "./common.css"

const Base = {
    siteUrl: ''
};

export {
    React,
    ReactDOM,
    Base,
    Http,
    ApiUrl,
    classNames,
    Row,
    Col,
    Button,
    Select,
    Space
};